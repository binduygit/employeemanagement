import { EmployeeExpenses } from './employee-expenses.model';

describe('EmployeeExpenses', () => {
  it('should create an instance', () => {
    expect(new EmployeeExpenses()).toBeTruthy();
  });
});
